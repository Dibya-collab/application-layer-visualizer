# AI Usage Log

**Platform:** OpenAI ChatGPT  
**Model:** GPT-5.6 Luna  
**Purpose:** Planning, implementation, debugging/iteration, documentation.

## Prompts / iterations
1. Asked the AI to explain the assignment requirements and convert them into a working-project plan.
2. Asked for a complete dual-panel architecture using Python + Flask + HTML/CSS/JavaScript.
3. Asked for the Browsing flow: DNS query/response followed by HTTP GET/response.
4. Asked for the Mail flow: SMTP EHLO, MAIL FROM, RCPT TO, DATA, message acceptance, QUIT.
5. Asked for the Streaming flow: DNS, HTTP manifest/playlist, and segment requests.
6. Asked for synchronized controls: pause, previous, next, replay, and automatic sequential reveal.
7. Asked for README, reflection, and a short demo script.
8. Reviewed the generated protocol sequences against the assignment requirement and retained this project as a simulation rather than claiming it creates real network traffic.

## AI evidence
For the final submission, attach screenshots/exported chat history showing the above prompts and the generated/iterated code. The assignment requires evidence such as prompt history, chat logs, agent artifacts, or screenshots.

## Human verification/corrections
- The application is explicitly labeled as a simulation.
- SMTP commands are shown in the required order.
- Streaming is represented as DNS + HTTP manifest + repeated HTTP segment requests.
- The dashboard has exactly two main panels at desktop width.
