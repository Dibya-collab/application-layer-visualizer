# AI Usage Log

**Platform:** OpenAI ChatGPT  
**Model:** GPT-5.6 Luna

## Project iterations
1. Planned the original Application Layer dual-panel dashboard.
2. Implemented simulated DNS, HTTP, SMTP and streaming flows.
3. Added a Transport Layer mode while preserving the Application Layer.
4. Added an interactive TCP/UDP simulator with segmentation, ports, sequence numbers, ACKs, loss/retransmission, checksum, flow control, congestion-control concept, multiplexing and demultiplexing.
5. Reviewed the UI so the project remains a two-panel dashboard.

## Important limitation
The Transport Layer features are educational simulations. They visualize protocol behavior rather than replacing the operating system's real TCP/UDP implementation.

For submission, retain screenshots or exported chat history as evidence of AI assistance, as required by the assignment.
